Implement Account module navigation + shared Timeline component (Phase 1.5), trust-first.

STRICT LANGUAGE RULES:
Use: campaign, commitment, escrow, refund, fulfillment, delivery, release
Avoid: order, checkout, cart, purchase, buy, deal, discount, flash

====================================================
A) ACCOUNT MODULE NAVIGATION (PHASE 1)
====================================================

Goal:
Create a consistent Account area with navigation:
- Profile
- Commitments
- Payments & Escrow
- Refunds
- Security (sessions + sign out everywhere)

Requirements:
1) Create an Account layout shell component:
- File: client/src/pages/account/layout.tsx (or client/src/layouts/account-layout.tsx)
- Desktop:
  - Left sidebar nav (vertical)
  - Right content area
- Mobile:
  - Top tabs or a simple menu (keep minimal)
- Nav items:
  1) Profile -> /account/profile
  2) Commitments -> /account/commitments
  3) Payments & Escrow -> /account/payments
  4) Refunds -> /account/refunds
  5) Security -> /account/security

2) Routing:
- Ensure /account redirects to /account/commitments (or profile; choose commitments as default)
- All /account/* routes must be protected by requireUserAuth (use existing auth guard)
- If user is not authenticated, redirect to /signin?next=<current>

3) Global Sign out availability:
- In Account shell header or sidebar, add "Sign out"
- Sign out should clear session cookie via existing endpoint (or add POST /api/auth/logout if missing)
- After sign out redirect to /signin

4) Security page:
- Minimal Phase 1:
  - Show current session status (“Signed in as <email>”)
  - Button: Sign out
  - If session listing exists in backend, show list; if not, show placeholder text:
    “Session management will expand in later phases.”
- No store language.

5) Keep existing pages:
- If an existing /account page exists, refactor into the new module routes without breaking.
- Preserve existing profile form and commitments list logic; just mount them under the new routes.

====================================================
B) SHARED TIMELINE COMPONENT (COST-SAVING)
====================================================

Goal:
Implement a reusable Timeline component used across:
- Commitment detail
- Payments & Escrow detail
- Refunds detail

Requirements:
6) Create: client/src/components/timeline.tsx
Export:
- Timeline component
- TimelineEvent type

TimelineEvent:
- id: string
- title: string
- timestamp: string | Date
- subtitle?: string
- status?: "neutral" | "info" | "success" | "warning" | "danger" (visual hint only)
- tags?: string[]
- meta?: Array<{label:string; value:string}>
- monospaceFields?: Array<{label:string; value:string}>
- details?: string
- isImmutable?: boolean (if true show small label “Append-only record”)

Timeline props:
- title?: string
- events: TimelineEvent[]
- density?: "compact" | "comfortable" (default "comfortable")
- showConnector?: boolean (default true)
- emptyStateTitle?: string
- emptyStateHint?: string

UI rules:
- Audit-style, trust-first
- Monospace for reference numbers / IDs
- No urgency UI
- Works inside right pane and mobile sheet

7) Integrate Timeline in ONE page as an example:
- Choose the Payments & Escrow detail page if it exists, otherwise Commitment detail.
- Map existing event/ledger data to TimelineEvent[] via a helper mapping function.
- Keep changes minimal.

====================================================
C) OUTPUT REQUIREMENTS
====================================================

Return:
- file paths created/changed
- new account routes list
- confirm account nav works (desktop + mobile)
- confirm Timeline compiles and is used in at least one page
- confirm store-brain test still passes
- run existing test suite and report results (at least the key scripts)
